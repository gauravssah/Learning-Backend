<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://cdn.tailwindcss.com"></script>
    <script src="https://kit.fontawesome.com/faa88aff29.js" crossorigin="anonymous"></script>
    <title>Welcome to KhataBook</title>
</head>

<body>

    <nav class="flex justify-between items-center bg-slate-800 px-4 py-3 text-white font-bold">

        <h1 class="text-2xl cursor-pointer"><a href="/">KhataBook</a></h1>

        <ul class="flex gap-3 ">
            <li class="hover:text-slate-300"><a href="/">Home</a></li>
            <li class="hover:text-slate-300"><a href="./create">Create</a></li>
        </ul>
    </nav>

    <h1 class="text-3xl font-bold m-3 ">All Hisaabs</h1>


    <div class="flex flex-col gap-3 m-3 sm:w-[90%] ">

        <% if(files.length> 0){ %>
            <% files.forEach(file=> { %>
                <div class="bg-gray-300 gap-3 py-2 px-2 flex items-center justify-between ">

                    <!-- title -->

                    <p class="text-xl font-bold">

                        <% const filetitle=file.split(".")[0] %>


                            <%= filetitle.charAt(0).toUpperCase() + filetitle.slice(1).toLowerCase() %>

                                <!-- view hisaab -->
                                <a href="/hisaab/<%= file %>">
                                    <i
                                        class="fa-solid fa-arrow-right text-sm ml-3 cursor-pointer bg-slate-800 text-white px-2 py-1 rounded-full hover:bg-blue-500">
                                    </i>
                                </a>
                    </p>


                    <!-- edit and delete -->
                    <div class="flex gap-3 items-center justify-between">
                        <a href="/edit/<%= file %>">
                            <i
                                class="fa-solid fa-pen cursor-pointer hover:bg-black bg-blue-500 text-white px-2 py-2 rounded"></i>
                        </a>

                        <a href="/delete/<%= file %>">
                            <i
                                class="fa-solid fa-trash cursor-pointer hover:bg-black bg-blue-500 text-white px-2 py-2 rounded"></i>
                        </a>

                    </div>

                </div>

                <% }) %>
                    <% } else { %>
                        <h3 class=" text-zinc-500">Currently Nothing to show..</h3>
                        <% } %>

    </div>








</body>

</html>